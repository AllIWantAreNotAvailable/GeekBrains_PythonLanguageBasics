# Создание обычной строки:
some_string = 'Hello, world'
print(f'Тип обычной строки "{some_string}" -> {type(some_string)}')

# Создание строки байт:
byte_string = b'Hello bytes'
print(f'Тип строки байт {byte_string} -> {type(byte_string)}')
